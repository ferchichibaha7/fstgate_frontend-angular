import { GetusernamePipe } from './getusername.pipe';

describe('GetusernamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetusernamePipe();
    expect(pipe).toBeTruthy();
  });
});
